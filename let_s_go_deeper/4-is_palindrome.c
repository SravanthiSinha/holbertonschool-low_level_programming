int str_len(char *str)
{
  int len;

  len=0;
  while(*(str+len)!='\0')
    len++;
  return len;
}

int checkpalindrome(char *s, int mid)
{
  if
  
}  


int is_palindrome(char *s)
{  
  int mid;
  
  mid=str_len(s)/2;
  if( mid %2 == 0)
  retrun checkpalindrome(s,mid));
}
